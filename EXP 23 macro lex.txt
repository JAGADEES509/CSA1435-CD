%{
	int m=0;
	int h=0;
%}

%%

[#define][ ][a-z 0-9]+ {m++;}
[#include][<][a-z]+[>] {h++;}
%%

int yywrap()
{}

int main(){
printf("Enter a statement: ");
yylex();
printf("No of headers: %d",h);
printf("No of macro: %d",m);
}

OUTPUT:

C:\Users\jagadees>set path=C:\Program Files (x86)\GnuWin32\bin

C:\Users\jagadees>flex macro.l.txt

C:\Users\jagadees>set path=C:\MinGW\bin

C:\Users\jagadees>gcc lex.yy.c

C:\Users\jagadees>a
Enter a statement:

#include <stdio.h>
#include <stdio.h>


int main() {
int main() {
    int num;
    int num;
    printf("Enter an integer: ");
    printf("Enter a: ");
    scanf("%d", &num);
    scanf("%d", &num);
    if (num % 2 == 0)
    if (num % 2 == 0)
        printf("%d is even.", num);
        printf("%.", num);
    else
    else
        printf("%d is odd.", num);
        printf("%.", num);
    return 0;
    retur;
}
}


No of headers: 0^C